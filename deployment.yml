apiVersion: apps/v1
kind: Deployment
metadata:
  name: inndb
spec:
  replicas: 2
  selector:
    matchLabels:
      app: inndb
  template:
    metadata:
      labels:
        app: inndb
    spec:
      containers:
      - name: inndb
        image: webapp:1
        # for local development against minikube registry
        #imagePullPolicy: Never        
        ports:
        - containerPort: 8080
        resources:
           limits:
             memory: 32Mi
             cpu: 125m
           requests:
             memory: 32Mi
             cpu: 125m
---
apiVersion: v1
kind: Service
metadata:
  name: hello-service
  labels:
    app: hello-service
spec:
  type: NodePort
  ports:
    # the port that this service should serve on
    - port: 80
      targetPort: 8080
  # label keys and values that must match in order to receive traffic for this service
  selector:
    app: inndb
---
apiVersion: v1
kind: Service
metadata:
  name: hello-service-cip
  labels:
    app: inndb
spec:
  type: ClusterIP
  ports:
    # the port that this service should serve on
    - port: 80
      targetPort: 8080
  # label keys and values that must match in order to receive traffic for this service
  selector:
    app: inndb
---
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: hello-ingress
spec:
  rules:
  - host: inndb.cluster-ingress-subdomain
    http:
      paths:
      - backend:
          serviceName: hello-service-cip
          servicePort: 80
        path: /
  tls:
  - hosts:
    - inndb.cluster-ingress-subdomain
    secretName: cluster-ingress-secret
